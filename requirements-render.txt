# Lightweight requirements for Render.com free tier (512 MB RAM)
# PyTorch/transformers (~2 GB) are excluded — VADER is used for sentiment instead.
# To run FinBERT locally, install the full requirements.txt instead.

fastapi==0.111.0
uvicorn[standard]==0.30.1
yfinance==0.2.40
curl_cffi>=0.7.0
pandas==2.2.2
numpy==1.26.4
pydantic==2.7.1
click==8.1.7
httpx==0.27.0
requests>=2.31.0
vaderSentiment>=3.3.2
newsapi-python>=0.2.7
